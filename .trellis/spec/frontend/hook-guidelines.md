# Hook Guidelines

> How hooks are used in this project.

---

## N/A — No Frontend

This is an STM32F407 embedded C firmware project. There are no React hooks or similar patterns.

The closest equivalent in embedded C is interrupt service routines and callback functions — see `Core/Src/stm32f4xx_it.c` for interrupt handlers and the backend guidelines for callback patterns.
